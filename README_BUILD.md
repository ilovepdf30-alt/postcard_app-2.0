# Postcard App — GitHub Actions build

1) Push to main.
2) Open Actions → Build Windows EXE.
3) Download artifact: postcard_app-windows-exe (contains dist/postcard_app.exe)

If build fails: open run logs → step "Build EXE (PyInstaller)".
